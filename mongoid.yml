production:
  sessions:
    default:
      uri: <%= Grams::Settings[:mongohq_url] %>
      options:
        skip_version_check: true
        safe: true
development:
  sessions:
    default:
      uri: <%= Grams::Settings[:mongohq_url] %>
      options:
        skip_version_check: true
        safe: true
